//pedir la temperatura y decir si hacer calor (mas de 30°C)

var temperatura = Number(prompt("Ingrese la temperatura: "))

if (temperatura >=30){
    alert("La temperatura es alta: hace calor ")
}else {
    alert("La temperatura es baja: no hace calor hace frio ")
}