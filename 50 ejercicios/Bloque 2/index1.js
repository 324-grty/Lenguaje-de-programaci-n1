//pedir la temperatura y decir si hace firio (menos de 15°C)

var temperatura = prompt("Ingrese la temperatura: ")

if (temperatura < 15){
    alert("La temperatura es bajas: hacer frio ")
}else {
    alert("La temperatura es alta: no hace frio ")
}